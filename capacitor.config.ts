import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dmitrygrigorov.flipflop',
  appName: 'Flip Flop',
  webDir: 'dist',
  backgroundColor: '#1e1147',
  android: {
    backgroundColor: '#1e1147',
  },
};

export default config;
